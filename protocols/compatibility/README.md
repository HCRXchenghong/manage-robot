# compatibility/ 兼容矩阵与 golden 向量

## 放什么

- **兼容矩阵**：哪种车端栈版本 + Adapter 版本 + 平台协议版本允许组合，
  哪些组合可以远驾、哪些只允许监控（对应架构文档 §6.4 服务端策略）。
- **golden 向量**：一批“标准答案”消息样本（字节级），
  任何端的编解码实现都必须和它完全一致，防止各端“各自理解”。

## 当前状态

协议草案 v0.1 刚建立，golden 向量在模拟器（阶段 0）完成后生成入库。
