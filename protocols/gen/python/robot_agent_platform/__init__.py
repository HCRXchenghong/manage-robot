"""Generated Python bindings for the platform.v1 wire protocol.

The Python import package is deliberately named ``robot_agent_platform`` to
avoid shadowing Python's standard-library ``platform`` module. The protobuf
descriptor package remains ``platform.v1`` for wire-level compatibility.
"""
